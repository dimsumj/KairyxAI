[project]
name = "kairyx-ai"
version = "0.1.0"
description = "KairyxAI demo deployment"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "alembic>=1.13",
    "cryptography",
    "fastapi",
    "google-generativeai",
    "gunicorn",
    "httpx",
    "numpy",
    "pandas",
    "PyJWT[crypto]>=2.10",
    "psycopg[binary]>=3.2",
    "requests",
    "sqlalchemy>=2.0",
    "uvicorn",
]

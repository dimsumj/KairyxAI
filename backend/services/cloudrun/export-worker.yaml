apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: export-worker
spec:
  template:
    spec:
      serviceAccountName: export-worker
      containers:
        - image: us-docker.pkg.dev/PROJECT_ID/kairyx/export-worker:latest
          command: ["uvicorn"]
          args: ["workers.export_worker_app:app", "--host", "0.0.0.0", "--port", "8080"]
          env:
            - name: APP_ENV
              value: prod
            - name: SERVICE_ROLE
              value: export-worker
          ports:
            - containerPort: 8080
